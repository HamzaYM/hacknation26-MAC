export default function Intake() {
  return (
    <div className="card">
      <h1>Intake — documents + voice interview</h1>
      <p className="todo">
        TODO(Suzy) — PRD §11 screen 2: drag-drop bill + EOB → Supabase Storage → parse progress;
        ElevenLabs voice-interview widget card (asks only what documents can&apos;t answer —
        income/household, lump-sum-today, monthly max). Data → documents + parse status.
      </p>
    </div>
  );
}
