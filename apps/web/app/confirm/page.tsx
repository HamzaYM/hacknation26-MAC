export default function Confirm() {
  return (
    <div className="card">
      <h1>Action Plan / Spec Confirm</h1>
      <p className="todo">
        TODO(Suzy) — PRD §11 screen 3: JobSpec rendered human-readably (fixture:
        <code> GET /api/cases/demo</code>); red-flag chips with dollar impact (&quot;Duplicate
        chest X-ray +$412&quot;); detected entities; ranged savings per lever; the boost panel
        (&quot;add income proof → unlocks charity screening&quot;); ONE button: &quot;Looks right —
        make the calls.&quot; Nothing dials before it (challenge gate).
      </p>
    </div>
  );
}
