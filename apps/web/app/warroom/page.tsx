export default function WarRoom() {
  return (
    <div className="card">
      <h1>War Room — the demo&apos;s money shot</h1>
      <p className="todo">
        TODO(Suzy) — PRD §11 screen 4: parallel call cards (one per entity) · dossier panel
        (armed levers + citations) · lever-ladder progress (rung_advanced events, §8.2 rungs) ·
        PRICE TICKER with delta badge readable from the back of the room · AI-disclosure
        indicator · outcome badges. Primary feed = typed milestone events on call_events via
        Supabase Realtime (lib/realtime.ts); transcript text is garnish. Golden-recording
        playback renders through this IDENTICAL card code path.
      </p>
    </div>
  );
}
