export default function Onboard() {
  return (
    <div className="card">
      <h1>Onboard / Authorize</h1>
      <p className="todo">
        TODO(Suzy) — PRD §11 screen 1: identity + insurance + call-auth credentials (account #,
        member ID); consent status chips (HIPAA ROI / insurer authorized-rep / recording —
        mocked statuses, honest UI); financial profile entry point. Data →
        job_spec.patient/insurance/authorizations.
      </p>
    </div>
  );
}
