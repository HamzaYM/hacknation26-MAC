// Geometry lifted from "ui mocks/Haggl Landing (5a + 5b).html"'s slash study:
// a steep coral bar (rotate ~-24deg) overshooting both stems of the H, with
// the H's own flat crossbar still reading through underneath — not a
// horizontal bar centered on the letter (an earlier draft got this wrong).
export default function Logo() {
  return (
    <span className="logo">
      <span className="logo-h">H</span>aggl
    </span>
  );
}
