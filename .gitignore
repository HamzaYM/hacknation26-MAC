.env
.env.local
node_modules/
.next/
__pycache__/
*.pyc
.venv/
venv/
dist/
build/
.DS_Store
apps/web/next-env.d.ts

# Large data files — raw MRFs, chargemasters, and pipeline intermediates
data/raw/
*.db
*.sqlite
*.csv.gz
*.zip
chargemasters*

# Local tooling + loose evidence at repo root (root-scoped with a leading slash
# so committed asset dirs stay tracked: deck-web-verify/, case-view-verify/,
# apps/web/public/** are all nested and unaffected).
/.claude
/.playwright-mcp
/*.jpeg
/*.png
/intake-parse-verified.png
/The Negotiator - Visual Brief (standalone).html
